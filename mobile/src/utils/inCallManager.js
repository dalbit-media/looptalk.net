// Native (iOS/Android) in-call audio routing, proximity sensor and
// keep-awake handling via react-native-incall-manager. A web stub exists
// since browser voice calls use the browser's own audio routing.
import InCallManager from "react-native-incall-manager";

export default InCallManager;
